OPEN BUGS
- Timer never stops at 0: the else branch in start() fires every second forever and re-saves isDone=true to Hive on every tick -> cancel the timer when remainingSeconds hits 0 and notify once
- pause() always ends up true -> should be a single isPaused = !isPaused
- Timers/controllers are never disposed (memory + battery leak)
- Can't delete a done task
- Task submission has no validation: can submit with 0 duration or no name

ARCHITECTURE / TODO
- Navigation hack: tapping a task pushes a whole new MainScreen just to switch tab -> switch the tab index via shared state instead
- Add a real Theme, remove hardcoded colors in shared/UI/colors.dart
- Put task storage behind a repository instead of touching Hive/HiveFunctions directly from widgets
- Replace print() debugging in hive_functions.dart with real logging

FIXED
- Timer indicator not updating until tabs were switched -> TimerController is now a true singleton, so the same object ticks and is listened to
- Today Screen crash when no task was set -> getPercentage() safely returns 0 when startingSeconds == 0, and task is now nullable
